# 安全政策（Security Policy）

本文件用于仓库由私有转为公开后，向发现安全问题的协作者说明上报通道与处理原则。公开前亦可参照。

## 支持版本

| 版本 | 是否维护中 |
|---|---|
| `main` 分支最新提交 | 是 |

## 上报漏洞

如发现安全漏洞，**请勿在公开 Issue 或讨论区披露细节**，以免被恶意利用。请通过以下私密通道上报：

- **GitHub 私密漏洞上报**（推荐，仓库公开后默认开启）：进入仓库 `Security` 选项卡 → `Report a vulnerability`，填写细节后仅维护者可见。
- **邮件上报**：可发送详情至通知邮箱 `1151374965@qq.com`，标题注明「[SECURITY] 仓库名 漏洞描述」。

请尽量提供：复现步骤、影响范围、已采取的临时缓解措施。维护者会在合理时间内确认并跟进。

## 凭据与密钥保护

- 本仓库严禁将任何密钥（API Key、Pixiv Cookie、`.env` 文件、GitHub Token）提交入库；`.env` 与 `.workbuddy/` 已在 `.gitignore` 中忽略。
- 如发现密钥意外入库，请**立即**通过上方私密通道上报，并就地轮换（作废 Pixiv 登录态、重置 API Key），不要只在公开 Issue 中提示。
- `动漫图片测试下载/` 目录原先含第三方版权样图（**不受** [LICENSE](./LICENSE) MIT 覆盖）；这些样图已从工作区与 git 历史彻底清除，目录现仅保留样例复现指引，转公开前无需额外处理。后续若自行添加媒体，请确保为原创或已授权，避免外泄。

## 归属

- 代码与文档版权见 [LICENSE](./LICENSE)（MIT，版权人 `duyv0826`）。
- 代码归属与审查责任人见 [CODEOWNERS](./.github/CODEOWNERS)。
